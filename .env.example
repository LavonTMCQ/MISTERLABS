# MISTERLABS TOPDOWN v1 - Environment Configuration

# Required: OpenAI API Key for GPT-4o model
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# Only OpenAI is used in this project. Do not set OpenRouter keys.

# Optional: PostgreSQL Database Connection (for database operations)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=""

# Optional: CoinMarketCap API Key (used by CMC tools)
CMC_API_KEY=""

# Optional: TapTools API Key (Cardano token prices/indicators)
TAPTOOLS_API_KEY=""

# Memory (LibSQL/Turso) — used by agent memory & vector search
# Prefer using these for cloud; falls back to file: URLs locally
MEMORY_DATABASE_URL=""
MEMORY_DATABASE_AUTH_TOKEN=""

# Optional: Task Complexity (for dynamic model selection)
# Values: high (uses GPT-4o) | low (uses GPT-4o-mini)
TASK_COMPLEXITY="low"

# Hyperliquid Trading Agent Configuration
# WARNING: Use a separate wallet for trading, NEVER your main wallet!
HYPERLIQUID_PRIVATE_KEY=""

# CoinGecko API (Free tier supported)
# Get from: https://www.coingecko.com/en/api
COINGECKO_API_KEY=""
