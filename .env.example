# MISTERLABS TOPDOWN v1 - Environment Configuration

# Required: OpenAI API Key for GPT-4o model
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# Optional: PostgreSQL Database Connection (for database operations)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=""

# Optional: Task Complexity (for dynamic model selection)
# Values: high (uses GPT-4o) | low (uses GPT-4o-mini)
TASK_COMPLEXITY="low"